{
  "name": "ehrtslib-demo-app",
  "version": "1.0.0",
  "description": "Demo web application for ehrtslib format conversion",
  "type": "module",
  "scripts": {
    "build": "deno task build",
    "dev": "deno task dev",
    "clean": "rm -rf dist/*"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "dependencies": {
    "fast-xml-parser": "^5.3.3",
    "yaml": "^2.3.4"
  }
}