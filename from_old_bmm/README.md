# Phase 1 Archive

The TypeScript files in this directory were generated from an older, less
information-rich BMM variant. They are kept here for comparison purposes.

The generation of these files was done by Jules, and the process can be seen in
the `feature-deno-library-generator` branch of the `code-generator` repository:
https://github.com/ErikSundvall/code-generator/tree/feature-deno-library-generator
