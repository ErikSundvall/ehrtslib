// Re-export wrapper for openehr_base
//
// Stable public path: the implementation lives in ./base/, split into one module
// per BMM package (see tasks/bmm_package_map.json).

export * from "./base/mod.ts";
