// Re-export wrapper for openehr_lang
//
// Stable public path: the implementation lives in ./lang/, split into one module
// per BMM package (see tasks/bmm_package_map.json).

export * from "./lang/mod.ts";
