// Re-export wrapper for openehr_term
//
// Stable public path: the implementation lives in ./term/, split into one module
// per BMM package (see tasks/bmm_package_map.json).

export * from "./term/mod.ts";
